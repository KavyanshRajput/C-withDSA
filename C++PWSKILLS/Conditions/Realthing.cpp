#include <iostream>
using namespace std;
int main(){
int a;
if (3+2%5) cout<<"This works....."<<endl;
if (a=10) cout<<"Even this works..."<<endl;
if (-5) cout<<"Surprizingly teven this works..";

    return 0;
}
//Why this code works even there is not any condition in if
//Because in if else any non zero number which is inside expression acts as true and printes the output..
